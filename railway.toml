[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "python3 -u backend/payroll_api.py"
healthcheckPath = "/api/lbd/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
